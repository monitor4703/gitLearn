git commands:

git <command>

config
init
status
add <file, files, dir, dirs>
add -A
commit -m "<text message>"
commit -a -m "..."
checkout
checkout -b <branch_name>
checkout <branch_name>
diff
push origin master
fetch
pull origin master

vim
Ctrl+~